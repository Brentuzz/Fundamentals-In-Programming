#include <stdio.h>

int main () {
    int i = 1;
    
    for(i = 1; i <= 5; i++){
    printf("%d ", i);
    }
    return 0;
}

/*This program starts with i = 1 and uses a for loop that runs
 while i is less than or equal to 5. Each time the loop runs, 
 it prints the value of i and then increases it by 1. The loop 
 stops once i becomes 6.
*/